/*
    cargo new <proj_name> - create a new rust project
    cargo build - to compile the project and create binary
    cargo run - to build & run the project. no build if no changes 
    cargo check - check if project compiles but dont run it
*/
fn main() {
    println!("Hello, !");
}
