// execution starts at main like normal
fn main(){
    // rust style is to add four spaces and not tabs
    // this is a macro and not a function
    // macros and functions may behave differently
    println!("prints a string");

    // compilation and running are separate
    // rustc main.rs compiles a binary executable

    // rust is compiled "ahead of time"
    // meaning that the binary executable is all that is needed to run it
    // and even the rust compiler is not needed to run the program then
}